bug at showPlayerReservations method in Reservations View:
    it only shows the first reservation
    it doesn't show the next reservations you make

adding, updating, deleting fields as field owner missing.


bugs:
    when reserving a field, if you enter 2-10-2024 it will show you reserved times at other dates as well.
    when the time you are trying to reserve is already reserved, it still says reservation added successfully.